torch>=2.0
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7

# Optional: approximate nearest-neighbour index for fleet-scale retrieval.
# The code uses exact brute-force cosine by default; FAISS is a drop-in for scale.
# faiss-cpu>=1.7
